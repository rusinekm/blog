  <%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, rows:20, cols:100 %>
  </div>  
   <div class="field">
    <%= f.label :tag_names %><br>
    <%= f.text_area :tag_names %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
